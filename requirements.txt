# Core dependencies
spacy>=3.8.0
nltk>=3.8.0
pydantic>=2.0.0
typer>=0.9.0
rich>=13.0.0
python-dotenv>=1.0.0

# Optional dependencies for full rendering
# mermaid-cli (install via npm: npm install -g @mermaid-js/mermaid-cli)
# graphviz>=0.20.0
