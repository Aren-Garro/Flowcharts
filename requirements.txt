# Core dependencies
spacy>=3.8.0
nltk>=3.8.0
pydantic>=2.0.0
typer>=0.9.0
rich>=13.0.0
python-dotenv>=1.0.0

# Document parsing
PyPDF2>=3.0.0
pdfplumber>=0.10.0
python-docx>=1.1.0
pyperclip>=1.8.0

# Web interface
flask>=3.0.0
flask-cors>=4.0.0

# === NEW: Phase 1 Optimization Dependencies ===

# Native Graphviz renderer (replaces Node.js mermaid-cli)
graphviz>=0.20.0

# Optional: Local LLM extraction (fee-free, API-independent)
# pip install llama-cpp-python instructor
# llama-cpp-python>=0.2.0
# instructor>=1.0.0

# Optional: D2 declarative renderer (modern aesthetics)
# System binary: https://d2lang.com/tour/install
# py-d2>=0.0.3

# Optional: Kroki unified renderer (requires Docker)
# docker run -d -p 8000:8000 yuzutech/kroki

# Legacy (still supported but no longer required):
# mermaid-cli (install via npm: npm install -g @mermaid-js/mermaid-cli)
